#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
spring:
  cloud:
    config:
      name: config-center
      profile: dev
#      uri: http://localhost:8400
      label: master
      discovery:
        enabled: true
        service-id: config-center
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8101/eureka/